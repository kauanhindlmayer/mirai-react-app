import { PagePlaceholder } from "@/components/page-placeholder"

export default function BoardsPage() {
  return (
    <PagePlaceholder
      title="Boards"
      description="Kanban board per team, selectable by backlog level."
    />
  )
}
