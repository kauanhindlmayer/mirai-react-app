import { PagePlaceholder } from "@/components/page-placeholder"

export default function TagsImportPage() {
  return (
    <PagePlaceholder
      title="Import Tags"
      description="CSV upload creates an async import job."
    />
  )
}
