import { PagePlaceholder } from "@/components/page-placeholder"

export default function TagsPage() {
  return (
    <PagePlaceholder
      title="Tags"
      description="CRUD table of tags with inline editing and bulk delete."
    />
  )
}
